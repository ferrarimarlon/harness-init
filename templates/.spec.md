# Especificação — [NOME DO PROJETO]

> Fonte única de verdade. Toda regra aqui é invariante do sistema. Código que quebra qualquer item deste documento está errado, mesmo que os testes passem.

## Domínio

TODO: descreva em uma frase o problema que este sistema resolve.

## Entidades

TODO: liste as entidades e seus campos.

Exemplo:
- **Recurso**: `id`, `nome`, ...

## Invariantes

> Cada invariante recebe um código (`I1`, `I2`, ...). Testes e código referenciam pelo código.

- **I1 — [título curto].** [descrição precisa da regra, sem ambiguidade]
- **I2 — [título curto].** ...
- **I3 — ...**

## Casos-fixtura obrigatórios

Cada invariante tem pelo menos um teste positivo (aceita) e um negativo (rejeita) em `tests/`. Nome do teste começa com o código: `I1 — [cenário]`.

## Fora de escopo

TODO: liste o que este sistema NÃO faz. Explicite pra evitar drift.

## Contrato de API interna

TODO: assinaturas das funções públicas do domínio.

```
funcao(input): Result<Ok, Erro>
```

Fluxo esperado retorna `Result<T, E>`. Exceção é bug, não fluxo.

## Definição de pronto

- Todas as invariantes têm teste passando.
- `.claude/hooks/check-spec-before-commit.sh` barra commit se qualquer teste de invariante falhar.
- Nenhum `TODO`, `FIXME` ou tipo genérico não justificado no código.
